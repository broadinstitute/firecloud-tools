from setuptools import setup

setup(name='firecloud-tools-lib',
      version='0.1',
      description='Base library for firecloud tools',
      url='http://github.com/broadinstitute/firecloud-tools',
      author='Broad Institute',
      packages=['lib'],
      zip_safe=False)